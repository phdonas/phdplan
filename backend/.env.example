# PHDPlan Environment Variables
# Copie este arquivo para .env e preencha os valores

# Ambiente (development ou production)
ENVIRONMENT=development

# URL do banco de dados
# Deixe vazio para usar SQLite local
# Em produção, o Render preencherá automaticamente
DATABASE_URL=

# URL do Frontend (usado para CORS em produção)
# Deixe vazio para aceitar qualquer origem (apenas em desenvolvimento)
FRONTEND_URL=

# Secret Key para JWT (gerado automaticamente, não altere)
# Em produção, o Render gerará uma chave segura
SECRET_KEY=sua-chave-secreta-aqui-mude-em-producao
